## Adding security measures:
- Adding timeout if passkey is entered 3 times wrong.
- Adding a timeout.php location for redirecting users to this automatically if the timeout session is still active and haven't waited.
- Adding IP ban of 3 days to 1 month if server detects same users with records of evading timeout.
- Session authentication will be replaced by a cookie session soon. Authentication of session ensures protected files like PHP, cannot be accessed normally. 

## Basic info:
If no symbol is in any item(on the end, then the to-do is ongoing.
If there's a symbol, i.e. a tick or a cross it indicates whether the to-do is finished or cannot be programmed if website hosting services have storage issues.
